<li>
  <%= question.title %></td>
  <%= question.body %></td>
  <%= link_to 'Show', question_path(question) %>
  <%= link_to 'Delete', question_path(question),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>
</li>