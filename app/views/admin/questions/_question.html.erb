<li>
  <%= question.title %>
  <%= question.body %>
  <%= link_to t('admin.tests.test.Show'), admin_question_path(question) %>
  <%= link_to t('admin.tests.test.edit') , edit_admin_question_path(question) %>
  <%= link_to t('admin.tests.test.delete'), admin_question_path(question),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>
</li>