<tr>
  <td><%= link_to truncate(gist.question.body, length: 25), [:admin, gist.question] %></td>
  <td><%= link_to gist_hash(gist.gist_url), gist.gist_url %></td>
  <td><%= gist.user.email %> </td>

  <td><%= link_to t('admin.tests.test.delete'), admin_gist_path(gist),
                  method: :delete,
                  data: { confirm: t('admin.tests.test.confirm')} %></td>
</tr>