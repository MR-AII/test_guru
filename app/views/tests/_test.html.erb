<tr>
  <td><b><%= test.title %></b></td>
  <td><%= test_level(test) %></td>
  <td><%= test.questions.count %> </td>
  <td><%= test.author.first_name %></td>

  <td><%= button_to t('helpers.button.test.start'), start_test_path(test), class: 'btn btn-outline-primary btn-sm' %></td>
</tr>

