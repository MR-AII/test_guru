<nav class="navbar navbar-light bg-light">
  <b><%= link_to 'Test Guru', root_path, class: 'navbar-brand' %></b>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <b><%= t('layouts.application.welcome_header', current_user: current_user.first_name, current_user_id: current_user.id) %> |</b>
      <%= link_to t('layouts.application.log_out'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t('devise.sessions.new.log_in'), new_session_path(resource_name) %> |
      <%= link_to t('devise.registrations.new.sign_up'), new_registration_path(resource_name) %>
    <% end %>
  </span>
</nav>